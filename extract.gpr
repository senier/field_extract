project Extract
is
   for Source_Dirs use ("src", "tests");
   for Object_Dir use "obj";
   for Main use ("tests.adb");
   for Create_Missing_Dirs use "True";

   package Compiler
   is
      for Switches ("Ada") use ("-gnata", "-gnato13");
   end Compiler;

end Extract;
