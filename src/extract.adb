package body Extract
is

   -------------
   -- Extract --
   -------------

   function Extract
     (Data   : Byte_Array;
      Offset : Natural) return U13
   is
      Result : U13 := 0;
   begin
      return Result;
   end Extract;

end Extract;
