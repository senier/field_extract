package body Extract
with
   SPARK_Mode
is

   -------------
   -- Extract --
   -------------

   function Extract
     (Data   : Byte_Array;
      Offset : Natural) return Value_Type
   is
      Result : Value_Type := 0;
   begin
      return Result;
   end Extract;

end Extract;
